# Copy to .env and fill in (or export in your shell) -- only needed for --refresh.
# Get a key at https://console.anthropic.com/settings/keys
# Note: a Claude.ai chat subscription (Pro/Max) does NOT include API access;
# this is billed separately, pay-as-you-go.
ANTHROPIC_API_KEY=

# Optional: override the extraction model (default: claude-opus-5)
# LEGALEAN_MODEL=claude-sonnet-5
